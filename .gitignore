# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# macOS
.DS_Store

# node
node_modules

# generated by `npm run types` (wrangler types) — regenerate, don't commit:
# it is ~half a megabyte of runtime type declarations.
worker-configuration.d.ts
