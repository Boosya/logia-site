/// <reference types="@cloudflare/vitest-pool-workers/types" />
// Declares the `cloudflare:test` module (env, SELF, createExecutionContext, …)
// that the Workers Vitest pool injects into the test isolate.
